/** Delay before hero stagger begins (matches legacy `navDelay`). */
export const NAV_DELAY_MS = 1000

/** Nav / menu entrance animation timeout on home (matches legacy `loaderDelay`). */
export const LOADER_DELAY_MS = 2000

/** Initial home splash overlay duration (legacy loader completes ~here). */
export const SPLASH_DURATION_MS = 2200
